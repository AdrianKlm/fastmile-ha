{
  "config": {
    "step": {
      "user": {
        "title": "Connect to FastMile router",
        "description": "Enter the router host, timeout in seconds, and polling interval in seconds.",
        "data": {
          "host": "Router host",
          "timeout": "Request timeout (seconds)",
          "scan_interval": "Polling interval (seconds)"
        }
      },
      "reconfigure": {
        "title": "Reconfigure FastMile router",
        "description": "Update the router host, timeout, or polling interval.",
        "data": {
          "host": "Router host",
          "timeout": "Request timeout (seconds)",
          "scan_interval": "Polling interval (seconds)"
        }
      }
    }
  }
}
