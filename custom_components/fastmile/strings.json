{
  "config": {
    "step": {
      "user": {
        "title": "Connect to FastMile router",
        "description": "Enter the router host, credentials, timeout in seconds, and polling interval in seconds.",
        "data": {
          "host": "Router host",
          "username": "Username",
          "password": "Password",
          "timeout": "Request timeout (seconds)",
          "scan_interval": "Polling interval (seconds)"
        }
      }
    }
  }
}
